# ── TiDB Cloud ────────────────────────────────────────────────────────────────
# Obtenha no painel TiDB Cloud → Connect → General
DATABASE_URL="mysql://USER:SENHA@HOST:4000/beyond_db?sslaccept=strict"

# ── Better Auth ───────────────────────────────────────────────────────────────
# Gere com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
BETTER_AUTH_SECRET="sua-string-aleatoria-longa"
BETTER_AUTH_URL="https://seu-dominio.vercel.app"

# ── Google OAuth ──────────────────────────────────────────────────────────────
# Crie em console.cloud.google.com → APIs & Services → Credentials
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# ── Resend (e-mails OTP) ──────────────────────────────────────────────────────
# Obtenha em resend.com → API Keys
RESEND_API_KEY=""
